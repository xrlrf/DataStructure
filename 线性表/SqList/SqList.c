#include<stdio.h>
#define MaxSize 50  // 定义线性表的最大长度
#define ElemType int

typedef struct{
    ElemType data[MaxSize]; // 顺序表的元素
    int length;             // 顺序表的当前长度
}SqList;

// 线性表的基本操作
// InitList(&L);
int main()
{

    return 0;
}